# Aview MES 迁移与现场部署手册

本手册适用于把 `av_icon` 项目和 MySQL 数据库从一台服务器电脑迁移到另一台电脑。目标电脑假定为全新的 Windows 电脑，未安装 Python。

## 1. 部署结构

一台服务器电脑同时运行以下服务：

- MES Web 服务：TCP `9000`，浏览器/前端访问。
- MQTT Broker：TCP `1833`，现场机台连接。
- MySQL：默认 TCP `3306`，仅 MES 程序访问。

三台机台只需要连接服务器电脑的 MQTT 地址，不需要直接连接 MySQL。

## 2. 旧服务器：迁移前准备

1. 记录旧服务器的局域网 IP、MySQL 用户名/密码、机台 MQTT 配置、当前型号和工单状态。
2. 停止 MES 服务，避免导出过程中继续写入生产数据。
3. 导出数据库。打开 PowerShell 或命令提示符后执行：

   ```powershell
   mysqldump -h 127.0.0.1 -u root -p --single-transaction --routines --triggers --databases aview_mysql > C:\transfer\aview_mysql.sql
   ```

   输入 MySQL 密码后会生成 `C:\transfer\aview_mysql.sql`。如 `C:\transfer` 不存在，请先创建该文件夹。

4. 复制整个 `av_icon` 文件夹和 `aview_mysql.sql` 到 U 盘、共享文件夹或新服务器。

> 不要依赖复制后的 `venv` 或 `.venv`：虚拟环境包含旧电脑的 Python 路径，在新电脑必须重新创建。复制它们不会影响源代码，但新服务器上应删除或忽略它们。

## 3. 新服务器：基础安装

1. 接入现场局域网，并为服务器电脑设置固定 IP。例如：`10.150.21.54`。实际 IP、掩码和网关应按现场网络规划设置。
2. 安装 **Python 3.10 x64**。安装时勾选 **Add Python to PATH**。
3. 安装 **MySQL Server 8.x**，设置并保存 MySQL 管理员密码。
4. 将项目复制到固定位置，例如：

   ```text
   C:\MES\av_icon
   ```

## 4. 恢复 MySQL 数据库

在新服务器的 PowerShell 中执行：

```powershell
mysql -u root -p < C:\transfer\aview_mysql.sql
```

输入新服务器 MySQL 的 root 密码。导入后验证：

```powershell
mysql -u root -p -e "SHOW DATABASES LIKE 'aview_mysql';"
```

应显示 `aview_mysql`。若没有，请停止后续操作，检查导出文件和 MySQL 导入报错。

## 5. 创建 Python 环境并安装依赖

在 PowerShell 执行（把路径替换为实际项目位置）：

```powershell
cd C:\MES\av_icon
python --version
python -m venv .venv
& .\.venv\Scripts\python.exe -m pip install --upgrade pip
& .\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

项目启动脚本已经直接使用 `.venv` 中的 Python，不需要执行 `Activate.ps1`，因此不会受到 PowerShell 脚本执行策略限制。

## 6. 修改本机配置

用文本编辑器打开以下文件，按新服务器实际情况修改。

### `appDjango/settings.py`

如果 MySQL 运行在本机，保留：

```python
MQTT_SERVER = '127.0.0.1'
MQTT_PORT = 1833

'HOST': '127.0.0.1'
'PORT': '3306'
```

将数据库用户名、密码改为新服务器实际 MySQL 账号。若 MySQL 安装在另一台电脑，`HOST` 必须改为那台数据库服务器的局域网 IP，并在 MySQL 中授权本服务器访问。

### `COMMON/setmqtt.py`

保持 Broker 监听：

```python
'bind': '0.0.0.0:1833'
```

这会使服务器在所有网卡上提供 MQTT 服务。

### `COMMON/SoftWare.ini`

将以下两项更新为新服务器的固定局域网 IP：

```ini
myip = 新服务器局域网IP
myip_line = 新服务器局域网IP
```

同时核对 `currentmodel`、`currentorder`、`line_no` 等生产状态，避免使用不属于当前产线的旧数据。

## 7. 数据库迁移状态检查

数据库是从已经可运行的旧服务器导入时，通常不需要再执行迁移。先检查：

```powershell
& .\.venv\Scripts\python.exe manage.py showmigrations
```

只有在确认迁移记录缺失、且已做好数据库备份时，才执行：

```powershell
& .\.venv\Scripts\python.exe manage.py migrate
```

不要在未导入旧库或不清楚现有表结构时盲目执行迁移。

## 8. 开放 Windows 防火墙端口

以**管理员身份**打开 PowerShell，执行：

```powershell
New-NetFirewallRule -DisplayName "Aview MES Web 9000" -Direction Inbound -Protocol TCP -LocalPort 9000 -Action Allow
New-NetFirewallRule -DisplayName "Aview MES MQTT 1833" -Direction Inbound -Protocol TCP -LocalPort 1833 -Action Allow
```

## 9. 启动与验收

双击 `start.bat`，或执行：

```powershell
& .\.venv\Scripts\python.exe manage.py runserver 0.0.0.0:9000 --noreload
```

以下日志表示核心服务已启动：

```text
Listener 'default' bind to 0.0.0.0:1833
Connected successfully
Starting development server at http://0.0.0.0:9000/
```

在服务器本机访问：

```text
http://127.0.0.1:9000/api/test
```

从同一局域网其他电脑访问：

```text
http://新服务器局域网IP:9000/
```

## 10. 机台设置

每台机台的 MQTT/Broker 地址设置为：

```text
新服务器局域网IP:1833
```

例如服务器 IP 为 `10.150.21.54`，则机台填：

```text
10.150.21.54:1833
```

不要填 `127.0.0.1` 或 `localhost`，因为它们在机台上会指向机台自己，而不是服务器。

如机台还需要调用 MES HTTP 接口，则使用：

```text
http://新服务器局域网IP:9000/
```

## 11. 常见问题

| 现象 | 原因与处理 |
| --- | --- |
| `ModuleNotFoundError` | 使用 `.venv` 中的 Python 重新执行 `pip install -r requirements.txt`。 |
| MySQL `Access denied` | `settings.py` 的账号密码与新 MySQL 不一致，先用 `mysql -u root -p` 验证。 |
| MySQL `timed out` | 数据库 IP、端口、网络或防火墙不通。 |
| MQTT `ConnectionRefusedError` | 确认 Broker 已监听 1833、未被其他程序占用、防火墙已放行。 |
| `DeviceClass` 为空 | `station_tab` / `equipment_tab` 没有导入业务数据或当前型号没有配置机台。 |

## 12. 迁移完成清单

- [ ] 已保存旧服务器数据库备份。
- [ ] 新服务器使用固定局域网 IP。
- [ ] MySQL 数据库 `aview_mysql` 已恢复。
- [ ] MySQL 账号密码已更新到 `settings.py`。
- [ ] `.venv` 已在新服务器重新创建并安装依赖。
- [ ] `SoftWare.ini` 的 IP 与生产状态已核对。
- [ ] 防火墙已开放 9000、1833。
- [ ] 三台机台均已指向 `新服务器IP:1833`。
- [ ] 已从其他电脑验证 Web 服务与机台 MQTT 连接。
