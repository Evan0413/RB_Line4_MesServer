# 现场数据库取数清单与导入步骤

目标：在只能短时间访问现场数据库时，优先拿到可以让 MES 前端、设备识别、工位规则与物料规则恢复运行的数据；不先复制大量产品测试历史。

## 先说结论

不要只复制“表结构”。对本项目而言，左侧菜单、登录账号、机台识别、型号/工位/绑定和物料规则都来自表内数据。

建议分两包导出：

1. **配置包（必须）**：量小，恢复菜单、用户、设备、型号、工位、物料和规则。
2. **当前生产状态包（按需）**：量通常不大，迁移发生在生产中时用于恢复当前工单、料盘、托盘和 MOM 待上传记录。

不需要历史追溯时，不导出产品码、逐站测试、统计和历史记录表。

> 当前本地表结构与代码已经发现过字段不一致。最稳妥的方式是让配置包同时包含**所选表的结构和数据**，以现场库为准重建这些小表；不要把现场库的数据硬塞进当前本地的不匹配表结构。

## A. 配置包：建议必取（15 张）

这些表不是大量生产数据，通常很小；它们决定系统能否正常显示菜单、登录、识别机台和执行生产规则。

```text
employee_tab
equipment_tab
group
menu
model_tab
station_tab
station_bind_tab
material_tab
planorder_partno_tab
planorder_partno_stpo_tab
planorder_partno_using_tab
planorder_laser_tab
mom_setting
viewboard_setting
tray_tab
```

| 表 | 为什么必须取 |
| --- | --- |
| `employee_tab` | 前端登录账号、密码和等级。 |
| `group`、`menu` | 左侧导航栏数据；不导入就会显示空导航栏。 |
| `equipment_tab` | MQTT 按机台 IP 识别设备。 |
| `model_tab` | 当前型号、SN 方式、合格/不良统计初始化。 |
| `station_tab` | 当前型号的工位、机台 IP 与工位子型号；启动时读取。 |
| `station_bind_tab` | 工位绑定/扫码规则。 |
| `material_tab`、`planorder_partno_*` | 型号/工位的料号、批次、点位及扫码字段规则。 |
| `planorder_laser_tab` | 镭雕码/支架码规则。 |
| `mom_setting` | 工厂、线别、SN/UUID 流水号状态。 |
| `viewboard_setting` | 看板当前显示的型号和工单。 |
| `tray_tab` | 工位与托盘/治具配置。 |

## B. 当前生产状态包：迁移发生在生产中时建议取（5 张）

这些不是大规模测试历史，但会影响在制工单、料盘、托盘和待补传状态。

```text
planorder_tab
planorder_stand_tab
pan_tab
panseries_tab
trayscan_tab
```

如现场还存在 MOM 未完成上传/重传的记录，再加上：

```text
mom_message
```

`ordersn_tab` 是每个产品的工单状态，规模可能很大。只有需要恢复正在生产、尚未完工的产品时才导出它；否则可不取。

## C. 可先不取的大量生产/追溯数据

以下表是历史产品、扫码、逐工位测试和统计。它们不决定菜单、账号或机台能否连接，但决定历史追溯和报表是否完整。

```text
ordersn_tab
qr_bind_tab
qr_confrimation_tab
qr_confrimation_tab_sp
product_statistics_tab
st_history_test_tab
st_re_tab
sta_total
sp20_test_tab
sp21_test_tab
sp22_test_tab
sp25_test_tab
st01_test_tab
st02_test_tab
st03_test_tab
st04_test_tab
st05_test_tab
st06_test_tab
st07_test_tab
st08_test_tab
st09_test_tab
st10_test_tab
st11_test_tab
st12_test_tab
st13_test_tab
st14_test_tab
st16_test_tab
st17_test_tab
st20_test_tab
```

`auth_*` 与 `django_*` 是 Django 系统表。当前前端使用 `employee_tab` 登录；不使用 Django Admin 时可以先不取这类表。完整迁移时仍建议整体备份。

## 1. 在现场服务器导出前的只读核对

先在现场 MySQL 中执行，记录关键表是否有数据：

```sql
USE aview_mysql;

SELECT 'employee_tab' AS table_name, COUNT(*) AS rows_count FROM employee_tab
UNION ALL SELECT 'menu', COUNT(*) FROM menu
UNION ALL SELECT 'group', COUNT(*) FROM `group`
UNION ALL SELECT 'equipment_tab', COUNT(*) FROM equipment_tab
UNION ALL SELECT 'model_tab', COUNT(*) FROM model_tab
UNION ALL SELECT 'station_tab', COUNT(*) FROM station_tab
UNION ALL SELECT 'station_bind_tab', COUNT(*) FROM station_bind_tab
UNION ALL SELECT 'mom_setting', COUNT(*) FROM mom_setting
UNION ALL SELECT 'planorder_tab', COUNT(*) FROM planorder_tab;
```

如有时间，再保存以下结构以便与本地比对：

```sql
SHOW CREATE TABLE station_bind_tab;
SHOW CREATE TABLE tray_tab;
SHOW CREATE TABLE planorder_laser_tab;
```

## 2. 在现场服务器导出配置包

先在现场电脑创建目录，例如 `D:\MES_Backup`。然后在命令提示符或 PowerShell 执行；`-p` 后不要填写密码，命令会交互要求输入。

```powershell
mysqldump -h 127.0.0.1 -u root -p --single-transaction --routines --triggers --add-drop-table aview_mysql employee_tab equipment_tab group menu model_tab station_tab station_bind_tab material_tab planorder_partno_tab planorder_partno_stpo_tab planorder_partno_using_tab planorder_laser_tab mom_setting viewboard_setting tray_tab > D:\MES_Backup\mes_config.sql
```

这会导出这 15 张表的**建表语句和数据**，并带有 `DROP TABLE IF EXISTS`。导入时会以现场库结构重建这些表。

> `group` 是 SQL 关键字附近的名称；在 Windows 的 `mysqldump` 表名参数中通常可直接使用 `group`。如命令报表名解析错误，将它改为 `` `group` ``，或从图形化工具导出该表。

## 3. 在现场服务器导出当前生产状态包（可选）

```powershell
mysqldump -h 127.0.0.1 -u root -p --single-transaction --routines --triggers --add-drop-table aview_mysql planorder_tab planorder_stand_tab pan_tab panseries_tab trayscan_tab mom_message > D:\MES_Backup\mes_runtime.sql
```

如果要保留在制产品，再单独导出 `ordersn_tab`，这样文件大小和恢复范围更容易控制：

```powershell
mysqldump -h 127.0.0.1 -u root -p --single-transaction --add-drop-table aview_mysql ordersn_tab > D:\MES_Backup\ordersn_current.sql
```

## 4. 复制文件

从现场电脑带走：

```text
mes_config.sql                 必须
mes_runtime.sql                若生产中迁移则建议
ordersn_current.sql            需要恢复在制产品时才带走
COMMON\SoftWare.ini            必须核对并保留
```

可使用 U 盘或受控共享文件夹。SQL 文件包含账号、设备 IP、工单及生产信息，应按现场数据安全要求保存。

## 5. 导入到当前本机前：先备份

在本机执行：

```powershell
mysqldump -h 127.0.0.1 -u root -p --databases aview_mysql > C:\MES_Backup\before_import.sql
```

确认备份文件非空后，再执行导入。导入 `mes_config.sql` 会替换该包列出的 15 张表，不会碰上面未列出的生产历史表。

## 6. 在本机导入

```powershell
mysql -h 127.0.0.1 -u root -p aview_mysql < C:\MES_Backup\mes_config.sql
```

如有状态包，再依次导入：

```powershell
mysql -h 127.0.0.1 -u root -p aview_mysql < C:\MES_Backup\mes_runtime.sql
mysql -h 127.0.0.1 -u root -p aview_mysql < C:\MES_Backup\ordersn_current.sql
```

导入后检查：

```powershell
mysql -h 127.0.0.1 -u root -p -e "USE aview_mysql; SELECT COUNT(*) AS menu_count FROM menu; SELECT COUNT(*) AS station_count FROM station_tab; SELECT COUNT(*) AS equipment_count FROM equipment_tab; SELECT COUNT(*) AS model_count FROM model_tab;"
```

## 7. 导入后的必做动作

1. 根据新现场实际网络，更新 `equipment_tab.equipment_ip` 与 `station_tab.equipment_ip` 中的**机台 IP**。服务器 IP 变更不应写到这两个字段。
2. 更新 `COMMON/SoftWare.ini` 的 `myip`、`myip_line` 为新服务器 IP。
3. 核对 `currentmodel`、`currentorder`、`currentorder_stand` 是否应沿用；若不是同一条生产线，先清空或通过系统正常切换。
4. 重启 `start.bat`。
5. 登录前端，确认菜单出现、设备列表出现；在连接所有机台前先用一台机台验证 MQTT 连接与扫码流程。

## 重要限制

- `--add-drop-table` 会删除并重建**命令中列出的表**。务必先完成本机 `before_import.sql` 备份。
- 若现场数据库的业务表结构与当前代码不一致，以现场“正在运行的同版本服务器”的库为准；导入后再用实际工位测试。
- 不要把“当前工单、在制产品、MOM 待上传记录”误当作普通配置删除；是否导出它们取决于是否需要中断后续产。
