# Aview MES 数据库表说明与迁移优先级

检查对象：`aview_mysql`  
检查日期：2026-08-18  
表数量：**60**  
当前状态：所有表的元数据行数均为 `0`，这与“只导入了表结构、未导入数据”的情况一致。

## 结论：迁移时不能只复制表结构

这套 MES 不只是把数据写到数据库；启动、机台识别、过站校验和工单生产都会读取数据库中的配置。因此，现场迁移至少需要恢复“必需配置”和“当前生产数据”。若还要保留可追溯性，则需要完整恢复历史生产数据。

建议最可靠的迁移方式是：从**同一套、正在使用的旧服务器**导出整个 `aview_mysql` 数据库（结构 + 全部数据），不要只导出表结构。

## 表从哪里来、何时写入

### 两种来源

1. **Django 框架表（10 张）**：`auth_*`、`django_*`。这些由 `manage.py migrate` 的 Django 迁移创建和维护，不属于 MES 业务规则。
2. **MES 业务表（50 张）**：其结构不在本项目的 Django migration 文件中；源码里也没有完整的 `CREATE TABLE` 脚本。它们应来自旧服务器数据库的初始化 SQL/完整备份，并由页面接口、MOM 对接或 MQTT 机台消息通过原生 SQL 读写。

因此，执行 `migrate` 只能补 Django 的系统表，**不能生成可用的 MES 设备、工位、物料、工单和追溯数据**。

### 业务表的写入来源与触发条件

| 表/表组 | 谁写入 | 触发条件 | 主要作用 |
| --- | --- | --- | --- |
| `employee_tab` | MES 用户管理页面 | 新增用户、修改等级或密码、删除用户 | MES 自定义登录账号及等级。 |
| `equipment_tab` | 设备管理页面 | 点击新增/编辑/删除设备 | 全局设备通讯簿；MQTT 客户端接入时按 IP 查找设备编号和名称。 |
| `model_tab` | 型号管理页面 | 新增型号；生产完成时更新 `Pass`/`Fail` | 型号主档、SN 方式、型号级良率计数。 |
| `station_tab` | 型号管理/换型页面 | 新增型号时从 `equipment_tab` 复制机台到该型号；换型时更新 `st_current_model` | “某型号有哪些工位、每工位当前跑什么子型号”的核心映射。启动时即读取。 |
| `planorder_partno_tab` | 新增型号、物料维护页面 | 新增型号先建空行；物料维护保存料号/批次 | 型号的 BOM/料号与批次数据。过站时会写入追溯。 |
| `planorder_partno_stpo_tab`、`planorder_partno_using_tab`、`material_tab`、`station_bind_tab` | 当前源码主要读取，未找到完整新增界面写入逻辑 | 通常由项目初始化、旧系统配置或数据库维护预置 | 工位物料、扫码字段和绑定等级/规则。缺失时会影响绑定、物料校验或过站。 |
| `mom_setting` | 当前源码主要更新，不负责创建第一条 | 每日首次取号时重置计数；镭雕/过站/切换工单时递增或交接 SN/UUID 计数 | MOM 工厂、线别及本地流水号状态；必须预先有一条正确记录。 |
| `viewboard_setting` | 换型页面 | 选择型号时更新 `model` | 看板当前显示型号/工单；也需要预先有行。 |
| `menu`、`group` | 当前源码主要读取（Django ORM） | 菜单初始化/旧库导入；当前版本未找到写入页面 | 前端菜单和图标分组，不影响 MQTT 机台通讯，但影响界面可用性。 |
| `tray_tab` | 治具/料盘维护页面 | 新增或给型号/工位配置托盘；删除时清理 | 型号、工位与托盘串配置。 |
| `trayscan_tab` | 托盘扫码接口 | 新增托盘时建立 NG 状态；扫码校验成功后更新为 OK；删除/解绑时清理 | 托盘当前扫码/校验状态。 |
| `pan_tab` | 料盘管理页面 | 手动新增/删除料盘 | 料盘主档。 |
| `panseries_tab` | MQTT/机台物料绑定流程 | 扫描料盘和产品码后，先清除该料盘旧关系，再插入新 `panno`–`sn` 关系 | 料盘与产品序列号关联。 |
| `planorder_tab`、`planorder_stand_tab` | 工单页面、MOM 工单拉取接口 | 手动新增工单或从 MOM 请求到工单；切换/关闭工单时更新状态、数量与时间 | 主产品/支架工单；保存计划数、实绩、状态、线别、料号和 SN 流水号。 |
| `planorder_laser_tab` | 镭雕规则设置接口/模板初始化 | 给型号保存或修改镭雕码规则 | 主产品与支架的镭雕码规则。 |
| `ordersn_tab` | MQTT 过站处理 | 首次扫码且校验通过时插入；后续过站更新当前站、时间、重测次数；最终测试更新结果/下线时间 | 每个产品当前生产状态，是在制品管理的核心表。 |
| `qr_confrimation_tab` | MQTT 扫码/过站处理，及手动补录接口 | 工位扫码/绑定校验通过时插入或更新；机台回传测试结果后更新 `test_result`、`test_time` | 主产品逐站追溯主表。 |
| `qr_confrimation_tab_sp` | 支架/特殊工位 MQTT 流程 | 支架码扫码通过时插入/更新；测试消息回传时更新结果 | 支架/特殊工位逐站追溯。 |
| `qr_bind_tab` | MQTT 或人工绑定操作 | 主码与附属码绑定成功时插入/更新；解绑/删除产品时清理 | 保存 QR、PCBA、Lens、支架码等产品码之间的绑定关系。 |
| `st01_test_tab` … `st20_test_tab`、`sp20_test_tab` … `sp25_test_tab` | MQTT 机台测试消息 | 机台上报测试开始/结束与项目值时，按 `Station_No` 动态写入对应 `STxx_test_tab`/`SPxx_test_tab`；单项回传再更新各测试字段 | 每一站的原始测试明细和项目判定。 |
| `product_statistics_tab` | MQTT 测试处理 | 产品达到最终测试结果时插入；返修/删除产品时删除或重算 | 按产品汇总的 OK/NG 统计明细。 |
| `st_history_test_tab` | 产品追溯保存接口 | 保存/归档产品追溯信息时插入 | 通用历史测试归档，`test_data` 可保存完整测试内容。 |
| `st_re_tab` | 当前版本未发现明确写入语句 | 应为旧版本的重测/返修过程表，需从旧库恢复后结合实际数据确认 | 重测、返修、跳站等历史记录。 |
| `sta_total` | MQTT 设备状态/计时逻辑 | 某工站首次统计时自动插入；正常、故障、停机状态变化时累计更新 | 月度工站运行、停机、故障时长及数量。 |
| `mom_message` | MOM 上传处理 | 产生待上传的产品/工站结果时插入；上传或重传成功后删除相应记录 | MOM 通讯待发送队列/记录。 |

### MQTT 生产写入的典型流程

```text
机台连接 Broker
  → 用机台 IP 查询 equipment_tab
  → 用当前型号查询 station_tab
  → 机台扫码/绑定校验
  → qr_confrimation_tab + ordersn_tab 写入或更新
  → 机台上传测试结果
  → STxx/SPxx_test_tab 写测试细节
  → 更新 qr_confrimation_tab 的测试结论
  → 最终工位：ordersn_tab 结案、product_statistics_tab 统计、mom_message 等待上传
```

## 影响数据补齐的实际风险

以下现象说明“当前代码”和“仅导入的表结构”很可能不是完全同版；在恢复旧库之前不要通过猜测字段来手动补数据。

| 对象 | 当前源码行为 | 当前表结构 | 风险 |
| --- | --- | --- | --- |
| `station_bind_tab` | 绑定流程会查询 `bind_level` | 只有 `gp_model`、`station_bind` | 调用绑定流程会报未知字段。 |
| `tray_tab` | `AddTray` 接口尝试写 `trayno` | 已见字段为 `tray_str`、`gp_model`、`equipment_num` | 该旧接口可能不可用；另一个 `AddTrays` 接口使用的是现有字段。 |
| `planorder_laser_tab` | 镭雕模板接口会读写 `code_rule`、`stand_code_rule` | 当前表结构仅显示若干 `*_code` 字段 | 镭雕规则配置可能在使用时因字段不匹配失败。 |

这些不是“填一条空数据”能解决的问题；应取得旧服务器上正在生产的完整数据库备份，再做结构比对。

## 表的分级

| 级别 | 含义 | 迁移建议 |
| --- | --- | --- |
| P0 | 服务启动、机台联机、生产规则必需 | 必须恢复数据并逐项核对 |
| P1 | 当前工单、物料/载具、生产过程所需 | 当前生产时必须恢复 |
| P2 | 生产追溯、测试明细、统计 | 业务启动可暂不恢复；追溯/报表必须恢复 |
| P3 | Django 框架内部表 | 通常随完整数据库一起恢复；不要只手动复制部分结构 |

## P0：必须恢复的配置与规则表

| 表名 | 用途 | 关键字段/规则 |
| --- | --- | --- |
| `equipment_tab` | 全局机台清单；MQTT 连接时按机台 IP 识别设备 | `equipment_name`、`equipment_num`、`equipment_ip`、`equipment_serial`、`equipment_check`。机台 IP 必须是现场实际地址。 |
| `station_tab` | 型号下的工位配置；服务启动会按当前型号读取该表 | `gp_model`、`equipment_num`、`equipment_ip`、`equipment_serial`、`st_current_model`、`bind_pass`。没有该表数据时日志会显示 `DeviceClass []`，机台不会被识别。 |
| `model_tab` | 产品型号主数据与型号规则 | `gp_model`、`sn_way`；`Pass`、`Fail` 也被程序用作型号统计计数。 |
| `station_bind_tab` | 型号的工位绑定/扫码规则 | `gp_model`、`station_bind`。用于决定各工位需要绑定、校验哪些码。 |
| `material_tab` | 工位/点位对应物料规则 | `station_no`、`point_no`、`material_no`、`num`。 |
| `planorder_partno_stpo_tab` | 型号、站点/点位、物料料号的配置 | `gp_model`、`station_point`、`PART_NO_NAME`、`model_name`、`agv_send`、`agv_get`。 |
| `planorder_partno_using_tab` | 工位使用哪个料号字段、批次字段、扫码字段的规则 | `stno`、`partno_name`、`partbatch_name`、`code_name`。 |
| `planorder_laser_tab` | 型号对应镭雕/支架码头规则 | `gp_model`、`head_code1`、`head_code2`、`stand_head_code1`、`stand_head_code2`。 |
| `mom_setting` | MOM 对接及当日 SN/UUID 计数器 | `sn_num`、`sn_num_stand`、`uuid_num`、`LINENO`、`LINENO_STAND`、`FACTORYCODE`。程序会按日期使用/重置其中的流水号。 |
| `employee_tab` | MES 自定义登录账号 | `userid`、`password`、`level`。 |
| `menu` | MES 菜单定义 | `menu_id`、`menu_name`、`menu_link`、`menu_group`。 |
| `group` | 菜单分组/界面分组 | `group_id`、`group_name`、`group_icon`、`group_main_id`。 |
| `viewboard_setting` | 看板当前显示的型号和工单 | `model`、`order`。 |
| `tray_tab` | 型号与机台对应的治具/托盘配置 | `gp_model`、`equipment_num`、`tray_str`。 |

### P0 之外、同样必须带走的文件配置

以下不在 MySQL 中，但同样决定系统如何运行：

| 文件 | 需要核对的内容 |
| --- | --- |
| `COMMON/SoftWare.ini` | `myip`、`myip_line`、`line_no`、`currentmodel`、`currentorder`、`currentorder_stand`、绑定码字段、重测站区间等。 |
| `appDjango/settings.py` | MySQL 地址/账号、`MQTT_SERVER`、MQTT 端口。服务器本机 MySQL/MQTT 时应是 `127.0.0.1`。 |
| `COMMON/setmqtt.py` | Broker 监听地址，应为 `0.0.0.0:1833`，让现场机台可连接。 |

## P1：当前生产必须恢复的业务表

| 表名 | 用途 | 迁移注意事项 |
| --- | --- | --- |
| `planorder_tab` | 主产品工单 | `order_no`、计划数量 `num`、实绩 `num_real`、型号、状态、上下线时间、料号、产线和 SN 流水号。当前主产品工单必须保留。 |
| `planorder_stand_tab` | 支架/辅线工单 | 与 `planorder_tab` 类似，但保存支架工单和 `sn_num_stand`。 |
| `planorder_partno_tab` | 型号/工单使用的料号及批次 | 每个 `gp_model` 的多个料号、批次字段；过站时会据此写入追溯记录。 |
| `ordersn_tab` | 每个产品码在工单中的当前状态 | 码值、`order_no`、在线/下线时间、`current_station`、`result`、`retest_num`、支架码等；当前在线产品不能丢失。 |
| `pan_tab` | 料盘主表 | `panno`。 |
| `panseries_tab` | 料盘与 SN 的关系 | `panno`、`sn`。 |
| `trayscan_tab` | 托盘扫码状态 | `tray_no`、`tray_status`、`gp_model`。 |

## P2：生产记录、追溯与统计表

这些表通常不会决定 Broker 是否启动，但会影响产品追溯、良率、下载报表和历史查询。需要追溯历史时必须一并恢复。

| 表名 | 用途 |
| --- | --- |
| `qr_bind_tab` | 产品各类码（QR、PCBA、Lens、支架码等）的绑定关系。属于生产过程记录，也可能被后续绑定校验查询。 |
| `qr_confrimation_tab` | 主产品每次过站的扫码确认、测试结果、时间、站点、料号与批次，是核心追溯表。 |
| `qr_confrimation_tab_sp` | 支架/辅线对应的过站确认与追溯记录。 |
| `product_statistics_tab` | 产品级通过/失败统计明细。 |
| `st_re_tab` | 重测/返修/跳站类的过程记录。 |
| `st_history_test_tab` | 通用历史测试归档；`test_data` 保存长文本测试数据，适合历史追溯。 |
| `sta_total` | 工站按年月累计的正常、故障、停机时长及数量统计。 |
| `mom_message` | 与 MOM 的消息/上传记录，包括时间、URL、站点、SN 和工单。 |

### 按工站拆分的测试明细表（共 21 张）

以下表是机台测试结果的原始明细。共同字段一般包括产品码、`station_no`、`test_result`、`gp_model`、`judge_code`、开始/结束时间；后续字段为该工站特有测试项目及其 `_Result` 判定。

| 工站类别 | 表名 | 说明 |
| --- | --- | --- |
| 普通工站 | `st01_test_tab`、`st02_test_tab`、`st03_test_tab`、`st04_test_tab` | 站别 01–04 的测试数据；包含灰阶、锁螺丝、点胶、SFR/位置等不同项目。 |
| 普通工站 | `st05_test_tab`、`st06_test_tab`、`st07_test_tab`、`st08_test_tab`、`st09_test_tab` | 站别 05–09 的温度、压力、扭矩、老化、泄漏等测试/过程数据。 |
| 普通工站 | `st10_test_tab`、`st11_test_tab`、`st12_test_tab` | 站别 10–12 的影像/电性/SFR、视场、标定参数等测试数据。 |
| 普通工站 | `st13_test_tab`、`st14_test_tab`、`st16_test_tab`、`st17_test_tab`、`st20_test_tab` | 其他站别的测试结果。 |
| 支架/特殊工站 | `sp20_test_tab`、`sp21_test_tab`、`sp22_test_tab`、`sp25_test_tab` | 支架/特殊流程的测试数据。 |

> 这些表属于生产数据，不是基础配置；但是如果代码在过站时先查历史以防重复过站或重复绑定，则当前未完工产品相关记录仍应保留。

## P3：Django 框架内部表（共 10 张）

| 表名 | 用途 | 建议 |
| --- | --- | --- |
| `auth_user`、`auth_group`、`auth_permission` | Django 后台账号、角色、权限 | 使用 Django Admin 时必须保留；本项目另有 `employee_tab` 自定义账号。 |
| `auth_user_groups`、`auth_user_user_permissions`、`auth_group_permissions` | 用户、角色、权限之间的关联 | 与上述权限表一起迁移。 |
| `django_admin_log` | Django 管理后台操作日志 | 可选历史数据。 |
| `django_content_type` | Django 模型元数据 | 应与代码/Django 版本匹配。 |
| `django_migrations` | 已执行迁移记录 | **必须与实际表结构一致**，不要只复制表结构而遗漏此表的数据。 |
| `django_session` | 登录会话 | 可不保留历史会话；表结构必须存在。 |

## 已发现的结构兼容性风险

当前代码在 `COMMON/msg_operation.py` 的绑定逻辑中会读取：

```sql
SELECT bind_level FROM station_bind_tab ...
```

但当前导入的 `station_bind_tab` 结构仅有：

```text
gp_model, station_bind
```

没有 `bind_level` 字段。该功能被调用时可能报“未知字段”错误。

这说明当前表结构很可能并非与当前代码完全匹配的版本。迁移时应优先导出旧服务器上正在运行这份程序所使用的完整数据库，而不是从其他来源拼接表结构。恢复后建议逐个核对 P0 表的字段和数据。

## 推荐恢复顺序

1. 备份当前新服务器数据库。
2. 从旧服务器导出完整 `aview_mysql`（结构 + 数据）。
3. 导入新服务器，并先确认 P0 表有记录：`equipment_tab`、`station_tab`、`model_tab`、`station_bind_tab`、`mom_setting`。
4. 核对 `COMMON/SoftWare.ini` 当前型号/工单与数据库一致。
5. 将每台机台的 MQTT 地址改为 `新服务器IP:1833`。
6. 启动服务；确认日志中的 `DeviceClass` 不再是空列表。
7. 用一台机台做扫码/过站测试，再接入全部机台。

## 可用于核对的只读 SQL

```sql
USE aview_mysql;

SELECT 'equipment_tab' AS table_name, COUNT(*) AS rows_count FROM equipment_tab
UNION ALL SELECT 'station_tab', COUNT(*) FROM station_tab
UNION ALL SELECT 'model_tab', COUNT(*) FROM model_tab
UNION ALL SELECT 'station_bind_tab', COUNT(*) FROM station_bind_tab
UNION ALL SELECT 'mom_setting', COUNT(*) FROM mom_setting
UNION ALL SELECT 'planorder_tab', COUNT(*) FROM planorder_tab
UNION ALL SELECT 'ordersn_tab', COUNT(*) FROM ordersn_tab;

SHOW COLUMNS FROM station_bind_tab;
```
